{
  "private": true,
  "scripts": {
    "prepare": "nitropack prepare",
    "dev": "nitropack dev --publicURL --qr --https --clipboard --open",
    "build": "nitropack build",
    "preview": "node .output/server/index.mjs",
    "test": "jest"
  },
  "dependencies": {
    "moment": "^2.30.1",
    "nitropack": "^2.8"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2"
  }
}
